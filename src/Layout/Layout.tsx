import React from "react";
import { Container } from "@mantine/core";

const Layout = () => {
  const layoutProps = {
    bg: "#dbe4ff",
    h: "100vh",
  };
  return <Container {...layoutProps}></Container>;
};

export default Layout;
